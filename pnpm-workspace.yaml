# The plugin repository is self-contained: devDependencies resolve the
# official harness client packages from the npm registry (published rc
# builds), so building or testing never touches a deepseek-harness checkout.
packages:
  - .
allowBuilds:
  esbuild: true
